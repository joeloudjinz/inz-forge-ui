{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "target": "es2022",
    "types": ["vitest/globals", "node"]
  },
  "include": [
    "**/*.spec.ts",
    "**/*.d.ts",
    // FIX: Ensure the setup file is part of the compilation context
    "./test-setup.ts"
  ]
}
